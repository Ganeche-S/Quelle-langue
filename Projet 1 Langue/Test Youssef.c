#include<stdio.h>
#include<stdlib.h>
/*
int main()
{
    int caract;
    FILE* fic = NULL;
    fic = fopen("Fichier1.txt","r");
    if (fic != NULL)
    {
        do{caract = fgetc(fic);
        printf("%c",caract);
        } while (caract != EOF);
            printf("\n");
            fclose (fic);
    } else
        printf("Ouverture impossible");

    return 0;
}
*/
